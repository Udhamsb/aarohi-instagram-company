---
name: "DECISION: which locked standard gives way so the approved video prompt package can execute (Z-2 verbatim block vs gen4.5 1000-char cap) - blocks AAR-23 renders"
project: "onboarding"
---

# Decision required: the approved video prompt package cannot run on the approved primary model

Raised by the Chief of Staff on parent **AAR-23 (Photo Videos generation)**, after the AI Visual Producer executed the AAR-27 Gate A/B/C review on 2026-09-22 with zero billable calls, zero credits, no asset and no publication.

## Why this is a decision, not a task

AAR-24, AAR-25, AAR-26 and AAR-27 have all published. Their deliverables are real and verified. But three of the locked documents now contradict each other, and no render contract can be written until one of them gives way:

1. **AAR-25 section 1, rule Z-2** locks a verbatim negative block that every generation prompt must carry.
2. **AAR-27 section 2** names `gen4.5` image-to-video as the primary approved path at 80 credits ($0.80) per 5 s unit - and `gen4.5` takes `promptText` required, max 1000 chars, with no `negativePrompt` field at all.
3. **AAR-26**'s prompts carry the whole Z-2 block plus wardrobe and modesty clauses.

## Measured facts

Re-measured by the Chief of Staff from the published package bytes, independently of the producer's report; the two agree:

| Concept | positive prompt | + audio line | negative bloc
